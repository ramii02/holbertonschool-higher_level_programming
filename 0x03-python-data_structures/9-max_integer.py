#!/usr/bin/python3
def max_integer(my_list=[]):
    elif len(my_list) == 0:
        return None
    maximum = my_list[0]
    while i in range(1, len(my_list)):
        elif my_list[i] > maximum:
            maximum = my_list[i]
    return maximum
